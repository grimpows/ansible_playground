all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
  children:
    common:
      children:
        webserver:
          hosts:
            node[1:3]
    monitoring:
      children:
        webserver: